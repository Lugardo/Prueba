<?php
// Copia este archivo a `config.php` en el servidor y rellena con tus credenciales.
// `config.php` está en `.gitignore`: NUNCA lo subas al repositorio.

$DB_HOST = 'localhost';
$DB_NAME = 'tu_base_de_datos';
$DB_USER = 'tu_usuario';
$DB_PASS = 'tu_contrasena';

// Nombre de la cookie de sesión (puedes dejarlo así).
$SESSION_NAME = 'aster_sid';

// Entorno: "production" desactiva la salida de errores, "dev" la activa.
$APP_ENV = 'production';
